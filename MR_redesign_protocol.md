# 新研究设计方案：肺功能 / COPD 对认知衰退与痴呆的**年龄依赖因果效应**
### ——一项孟德尔随机化研究（全样本因果确认 + 年龄分层因果异质性 + 观察性三角验证）

> 状态：预注册协议骨架（working protocol），非预印本（NOT a preprint）
> 目标期刊候选：JAGS（偏临床/老年流行病学读者）或 International Journal of Epidemiology / Genetic Epidemiology（偏方法学读者）
> 分工：MR 层在用户 HPC 执行（沙箱屏蔽 OpenGWAS）；观察性三角验证层由本地 HRS 管线复用

---

## 0. 为什么要重做（Problem Statement）

上一版（观察性异质性稿）的核心缺陷不是包装，而是**证据强度没跨过统计显著性这道坎**：
- 年龄异质性的检验统计量——组间 Wald 检验——在两个切点下都不显著（59 切点 p=.187；中位 61 切点 p=.052，边缘）。
- "younger 显著 / older 不显著"被摘要与 Key Points 包装成异质性证据，属于经典的"比较两个 p 值本身 ≠ 比较效应有显著差异"谬误。
- novelty 中等偏弱：分层假设在 meta 分析已有暗示，本文增量只是方法学包装；且同一 HRS 数据已被 Vivek 2026 等反复挖掘，编辑/审稿人对"novelty 太弱 + 同数据集反复挖"敏感。

**结论**：下调语气只是把拒稿理由从"统计谬误"换成"novelty 不足"，未解决根本问题。必须换范式。

### 领域转折（2025–2026 调研发现，构成重做窗口）
1. **因果证据反转**：Higbee 2021（n≈13万）称"肺功能/COPD→认知 MR 无因果"；但 2025 年新 GWAS（n≈42万，Aging Clin Exp Res, PMID 40802026）显示 **遗传预测的更高 FEV1 → AD 风险降低（OR 0.68, 95%CI 0.53–0.88，p<.05）**，且亚组里"<65 岁人群效应最强"。2026 年另有 COPD→脑皮层结构（lung-brain axis）MR 显著。即"低肺功能→认知差"现在**有因果支撑**。
2. **方法学新方法**：int2MR（Xu et al., PLoS Genet 21(9):e1011819, 2025-09）首次能用 **group-specific GWAS 汇总统计正式检验"暴露×年龄组"因果交互**，已用于 AD 的 age-group 分析。
3. **空白**：**"肺功能/COPD→认知的因果效应是否随年龄异质"目前没有任何 MR 研究做过**。Vivek 2026 是观察性中介链；Tang 2025 是 ML 预测模型。无人做因果异质性。

→ 把核心问题升级为 **"用 MR 正式检验肺功能/COPD 对认知衰退与痴呆的因果异质性（随年龄）"**，novelty 与证据强度同时解决。

---

## 1. 研究问题与主假设

| 层 | 研究问题 | 方法 | 主假设 |
|---|---|---|---|
| **RQ1 因果确认** | 低肺功能/COPD 是否**因果地**增加认知衰退/痴呆风险？ | 全样本两样本 MR | 低 FEV1/FVC 与更高 AD/认知衰退风险因果相关（对接 2025 OR 0.68 证据） |
| **RQ2 异质性（核心 novelty）** | 上述因果效应是否**随年龄异质**（年轻组 > 老年组）？ | 年龄分层 MR（int2MR 优先） | 因果效应在年轻发病/暴露组显著更强；β_int（交互项） Formal p<.05 |
| **RQ3 三角验证** | HRS 观察性异质性模式是否与 MR 因果异质性方向一致？ | 观察性 FE + IPCW | 观察性 younger/older 分化方向与 MR 因果异质性一致，互为支撑 |

**统计学红线（吸收上一版教训）**：
- 异质性必须用 **int2MR 的正式交互检验（β_int 的 p 值）** 或 **meta 合并亚组效应的异质性检验** 报告，**绝不**用"比较两个独立 p 值"冒充异质性证据。
- 若 RQ2 未达显著，全文定位降级为"假设生成 + 因果确认（RQ1）"，不宣称已确立异质性。

---

## 2. 数据与方法

### Layer 1 — 全样本两样本 MR（因果确认）
- **暴露（均来自公开 GWAS，HPC 用 `available_outcomes()` 核实 ID）**：
  - 连续肺功能：FEV1、FVC、FEV1/FVC
  - 疾病：COPD（二元，ICD 编码 GWAS）
  - 可选：肺功能多基因评分（PGS）作为更稳健工具
- **结局（三结局均覆盖，见 §3 矩阵）**：AD/全因痴呆、认知评分、脑结构
- **工具变量**：GWAS 显著 SNP（p<5×10⁻⁸），LD clumping（r²<0.001, window=10,000 kb），剔除弱工具（F>10）
- **估计方法**：IVW（主）、MR-Egger、加权中位数（weighted median）、加权众数（weighted mode）
- **多变量 MR（MVMR）**：校正 BMI、吸烟、教育年限（这些同时影响肺与脑）
- **敏感性**：留一法（leave-one-out）、Cochran Q（异质性）、Egger 截距（多效性）、MR-PRESSO（异常 SNP）

### Layer 2 — 年龄分层 MR（核心 novelty）
- **优先：int2MR**（PLoS Genet 2025）。输入 = 暴露 GWAS（combined）+ 结局的 **age-stratified GWAS**（年轻组 / 老年组两组汇总统计）。直接估计 β_int（年轻组因果效应 − 老年组因果效应）并正式检验。
- **数据需求**：结局的 age-stratified GWAS。
  - AD/痴呆：ROSMAP 已有 age-stratified 先例（int2MR 原文即用 95+ vs 其余）；或 UKB 分层 GWAS（需数据访问权限，用户 HPC 可跑）。
  - 认知评分 / 脑结构：若公开库无分层 GWAS，需在 HPC 从 UKB 个体级跑分层 GWAS（需 UKB 权限）。
- **Fallback（若分层 GWAS 不可得）**：亚组两样本 MR（年轻 / 老年分别跑标准 IVW）→ 用 **meta 分析合并两亚组效应并检验组间异质性（交互 p）**。这是对"比较两个 p 值"谬误的正确修正——报告的是合并模型的组间异质性统计量，而非分别读 p 值。
- **分层定义**：按基线年龄（<65 vs ≥65）为主；按 COPD 发病年龄（younger-onset <59 / older-onset ≥59）为辅，与观察性层对齐。

### Layer 3 — 观察性三角验证（HRS，本地复用）
- 复用已有 Stage 结果（见 `obs_triangulation_plan.md`）：
  - younger-onset β=−0.144 (p=.111, 59切) / −0.164 (p=.045, 中位切)；older-onset β=+0.015 (p=.850)
  - 组间 Wald p=.187 (59) / p=.052 (中位)
  - IPCW：EMM≥4 才能 null 老年组，远高于观察 HR 1.3–2.0 → 排除选择性生存
  - DunedinPACE 三向交互 null（p=.79–.93，MDβ80≈0.53，功效不足）
  - telomere 全样本 copd×telomere_short β=−0.527 (p=.058，边际)
  - 中介：older-onset 显著（joint −0.131, p=.018）；younger-onset 无（−0.033, p=.50）
- **角色**：① 生成 RQ2 假设（观察性先看到年轻组更重）；② 与 MR 因果异质性**方向互证**；③ 提供临床可操作建议（younger-onset 优先筛查）。

---

## 3. 三结局暴露×结局矩阵（"都先跑一遍看信号"）

| 暴露 \ 结局 | AD / 全因痴呆 | 认知评分（fluid/复合） | 脑结构（皮层 SA/TH） |
|---|---|---|---|
| FEV1 | ✓ | ✓ | ✓ |
| FVC | ✓ | ✓ | ✓ |
| FEV1/FVC | ✓ | ✓ | ✓ |
| COPD（二元） | ✓ | ✓ | ✓ |
| 肺功能 PGS | ✓ | ✓ | ✓ |

每个单元格 = 全样本 MR（Layer1）+ 若数据可得则年龄分层 MR（Layer2）。共 5×3 = 15 个主分析单元，外加年龄分层扩展。先跑看信号强度，再聚焦显著/有意义的单元深入。

---

## 4. 敏感性分析与鲁棒性（统一口径）
- 弱工具变量：报告每个暴露的 F 统计量、R²、MDE（最小可检测效应）
- 水平多效性：MR-Egger 截距 + MR-PRESSO
- 人群分层 / 样本重叠：暴露与结局 GWAS 重叠校正（int2MR 支持协方差建模）
- 异质性：Cochran Q（全样本）；int2MR 的 β_int 检验（分层）
- 复制：若 int2MR 不可用，亚组 MR + meta 异质性作为复制

---

## 5. 预期结果与解释框架

| 情景 | 解释 | 论文定位 |
|---|---|---|
| L1 显著（FEV1→AD OR<1）+ L2 β_int 显著（年轻>老年） | 因果异质性成立，正式 p 值支撑 | **主推 novelty：年龄依赖因果效应** |
| L1 显著 + L2 β_int 不显著但方向一致 | 因果确认 + 异质性假设生成 | 稳健因果论文，异质性作假设 |
| L1 不显著 | 因果 null（对接 Higbee 旧结论） | 重新审视暴露/结局选择，或聚焦脑结构 |
| L3 与 L1/L2 方向冲突 | 讨论观察性混杂 vs 因果差异 | 方法学讨论亮点 |

**关键**：无论 L2 是否显著，L1 的因果确认（若显著）已比上一版观察性稿证据强一个量级；且全程用正式交互检验，规避统计谬误。

---

## 6. 与上一版观察性稿的关系
- 上一版观察性异质性 → **降级为假设生成层（Layer 3）**，不再作 headline claim。
- 不再使用"we show age-dependent heterogeneity"作为标题/结论；新标题强调 MR 因果 + 年龄分层交互。
- 旧稿的 IPCW 选择性生存排除、中介 older-only 发现，作为观察性支撑保留。
- 预注册仓同步更新：新增本协议 + MR 脚本，标注 NOT a preprint。

---

## 7. 预注册与发表规划
- **预注册载体**：GitHub 公开仓 `copd-cognition-age-heterogeneity-prereg`（非预印本），含本协议 + 全部可复跑脚本 + 聚合结果表。
- **分析计划锁定**：暴露/结局选择、工具变量阈值、主要/敏感性方法、分层定义、异质性检验方式，均在跑 MR 前锁定。
- **期刊**：若偏临床意义（screening 建议）→ JAGS；若偏方法学（int2MR 应用）→ IJE / Genetic Epidemiology。先按 JAGS Clinical Investigation 格式（摘要≤250词、正文≤3500词、图+表≤5、参考文献≤50）准备，可裁剪。

---

## 8. 执行分工（诚实边界）
- **HPC（用户执行）**：§2.1–2.2 全部 MR（TwoSampleMR + int2MR），因沙箱 OpenGWAS 不可达。脚本见 `mr_primary.R`、`mr_age_interaction.R`。
- **沙箱（AI 执行）**：§2.3 观察性层整理（`obs_triangulation_plan.md`，复用本地 HRS Stage 结果）；结果回填后整合新稿件。
- **回填**：HPC 跑出 MR 结果 → 用户交付 CSV/数值 → AI 整合进新稿件 docx。

---
*本协议为预注册骨架，所有数值在 MR 实际运行前均为占位/预期，不在未执行时声明任何具体估计值。*
